<%= render "menu" %>

<div class="content">
  <%= render "info" %>

  <div class="recent_topics box">
    <h2 class="title">受大家喜歡的話題</h2>
    <ul>
      <% @topics.each do |item| %>
        <li>
          <span class="node"><%= render_node_name(item.node_name,item.node_id) %></span>
          <%= render_topic_title(item) %>
          <span class="info pull-right"><%= item.likes_count %> 人喜歡, <%= item.replies_count %> 則回應</span>
        </li>
      <% end %>
    </ul>
  </div>
  <div class="box">
    <h2 class="title"><%= t("topics.recent_replies")%></h2>
    <%= render "replies", :replies => @replies %>
  </div>



</div>
<%= render "sidebar", :user => @user %>
